# embry0 frontend env vars. Copy to .env (gitignored) for local dev.
# All VITE_* vars are inlined into the bundle at build time.

# WebSocket auth: must match the backend API_KEY. Required in production
# (when AUTH_DEV_MODE=false on the backend). Leave empty in dev.
VITE_API_KEY=
