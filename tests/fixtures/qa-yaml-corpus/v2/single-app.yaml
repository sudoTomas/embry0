version: 2

workspace_provider:
  type: npm-workspaces-turbo
  config: {}

defaults:
  mode: process
  sandbox_profile: slim
  ready_checks:
    - http: "http://localhost:3000"
      expect_status: 200
      expect_body_regex: null
  boot_timeout_seconds: 300
  acceptance_criteria_template:
    - "page loads"

qa_required: auto

apps:
  app:
    boot_command: "npm run dev"
    frontend_url: "http://localhost:3000"
